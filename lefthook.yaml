pre-commit:
  jobs:
    - run: mise run //packages/...:check
    - run: mise run //packages/...:test
commit-msg:
  jobs:
    - name: Validate commit message
      run: cat {1} | convco check --from-stdin
